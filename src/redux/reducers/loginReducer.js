//import { types } from "../types/types";

export const loginReducer = ( state={}, accion  ) => {

    // switch (accion.type) {
    //     case types.login:
            
    //     return {
            
    //     }
    // }
}